\documentclass[12pt, titlepage]{article}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{fancyhdr}
\usepackage[bidi=basic]{babel}
\babelprovide[main,import]{hebrew}
\babelfont[hebrew]{rm}{Hadasim CLM}
\babelfont[hebrew]{sf}{Miriam Mono CLM}
\usepackage{titlesec}
\title{מושגי יסוד במתמטיקה 01040002 \\ גליון 3}
\author{יונתן אבידור - 214269565}
\titleformat{\section}{\Large\sffamily\bfseries}{\thesection}{1em}{}
\begin{document}
\maketitle
\pagestyle{fancy}
\fancyhead{}
\fancyfoot[L]{\text{214269565}}
\part*{שאלה 1}
נתונות שלוש קבוצות $X,Y,Z$. הוכיחו או הפריכו כל אחת מהטענות הבאות:
\section*{סעיף א'}
אם $X \in Y$ וגם $Y\subseteq Z$ אז $X\in Z$.
\section*{סעיף ב'}
אם $X\in Y$ וגם $Y \subseteq Z$ אז $X\subseteq Z$.
\section*{סעיף ג'}
אם $X \subseteq Y \cap Z$ אז: $X \subseteq Y$ וגם $Y\subseteq Z$.
\section*{סעיף ד'}
אם $X \subseteq Y \cup Z$ אז: $X \subseteq Y$ וגם $X\subseteq Z$.
\section*{סעיף ה'}
אם $X\cup Z = Y \cup Z$ אז $X=Y$.
\newpage
\part*{פתרון 1}
\section*{סעיף א'}
כן!
$$
Y\subseteq Z \implies \forall X\in Y : X\in Z
$$
\section*{סעיף ב'}
לא!
$$
X=\{1\},Y=\{\{1\}\}Z=\{\{2\},\{1\}\}
$$
\section*{סעיף ג'}
לא!
$$
X=\{1\},Y=\{1,2\},Z=\{1\}
$$
\section*{סעיף ד'}
לא!
$$
X=\{1,2\},Y=\{2\},Z=\{1,3\}
$$
\section*{סעיף ה'}
לא!
$$
X=\{1\},Y=\{1,2\},Z=\{1,2,3\}
$$
\newpage
\part*{שאלה 2}
\section*{סעיף א'}
הראו: קיימת ויחידה סדרת קבוצות $A_0,A_1,A_2,\ldots$ המקיימת את התכונות (א), (ב), (ג) שלהלן:
\subsection*{(א)}
לכל $i=0,1,2,\ldots$ מתקיים
$$
\left|A_i\right|=i
$$
\subsection*{(ב)}
לכל $i=0,1,2,\ldots$ מתקיים
$$
A_i \subseteq A_{i+1}
$$
\subsection*{(ג)}
לכל $i=0,1,2,\ldots$ מתקיים
$$
A_i \in A_{i+1}
$$
\section*{סעיף ב'}
הראו שהסדרה שהגעתם בסעיף הקודם יחידה, במובן הבא:\\
נניח ש-$B_0,B_1,B_2,\ldots$ סדרה נוספת המקיימת את התכונות (א)(ב)(ג)\\
אז לכל $i=0,1,2,\ldots$ מתקיים $A_i=B_i$
\newpage
\part*{פתרון 2}
נבנה קבוצה כנדרש\\
על פי תכונה (א), $\left|A_0\right| = 0$. הקבוצה היחידה שעוצמתה $0$ היא הקבוצה הריקה ($\emptyset$)\\
לכן $A_0 = \emptyset$, 
\end{document}
